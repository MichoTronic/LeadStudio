{
  "name": "timeless-lead-studio",
  "version": "5.0.0",
  "private": true,
  "scripts": {
    "test": "npm --prefix functions test",
    "test:coverage": "npm --prefix functions run test:coverage",
    "check": "node --check functions/index.js && node --check public/config.js && node --check public/export-utils.js && node --check public/lead-list-utils.js && node --check public/app.js && node --check scripts/browser-smoke.js && node --check scripts/google-client-live-smoke.js && npm test",
    "audit": "npm audit --prefix functions --omit=dev",
    "outdated": "npm outdated --prefix functions",
    "smoke:browser": "node scripts/browser-smoke.js",
    "smoke:google-clients": "node scripts/google-client-live-smoke.js"
  }
}
